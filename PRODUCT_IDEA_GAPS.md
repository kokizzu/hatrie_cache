# Product Idea Gaps

This is the current implementation queue for ideas compared with ClickHouse,
Materialize, and Tarantool. It contains 50 candidate gaps for each product.
The audit was performed against the exported Go packages, S| T-U44 | Slab fragmentation diagnostics | Implemented: opt-in portable heap placement, allocator metadata, reusable idle bytes, and stack footprint from runtime counters. | Platform portability and sampling cost; no default-path work or measured allocations. |L surface, server
commands, monitoring APIs, backup/restore paths, and current documentation on
2026-09-12. A row is retained only when the repository has no complete,
end-to-end equivalent; a specialized or opt-in partial implementation is
called out in the gap column.

These are candidates, not promises. Each implementation must add a focused
test first, run the failing test, implement the smallest complete contract,
run package/race/vet/full verification, and benchmark the intended workload.
Features stay opt-in unless the measured default path is unchanged. A feature
is removed from this file or marked adopted only after its behavior and
tradeoffs are documented and its commit is published.

## ClickHouse

| ID | Candidate | Current gap | Adoption gate |
|---|---|---|---|
| CH-U01 | Durable asynchronous-insert deduplication | Partially adopted by `hatCache.AsyncInsertBuffer`: keyed journaled writes use the durable command-journal fingerprint ledger and survive replay; the generic `hatPipeline.AsyncBatcher` and server-level async-insert queue lifecycle remain caller-owned. | Crash/restart replay, bounded ledger memory, eviction horizon, and duplicate-byte measurements. See [CHU01_DURABLE_ASYNC_INSERT_DEDUP.md](CHU01_DURABLE_ASYNC_INSERT_DEDUP.md). |
| CH-U02 | Unified external `ORDER BY` spill | Adopted for direct `EXTERNAL('name')` sources when the resolver implements `ExternalStreamSourceResolver`; materialized resolvers retain the established fallback. | Stable ties, NULL/collation behavior, cancellation cleanup, disk quota, and RSS versus CPU. See [CHU02_EXTERNAL_ORDER_SPILL.md](CHU02_EXTERNAL_ORDER_SPILL.md). |
| CH-U03 | Versioned partial aggregate wire states | `hatDataStructure.AggregateStateEnvelope` now provides bounded, checksummed HAG1 envelopes, and S| T-U48 | Idempotent remote-call retry policy | Peer calls lack a method-aware retry/backoff policy tied to idempotency keys and fencing tokens. | No duplicate mutation, jitter, and observability. |L exposes mergeable HLL and t-digest state pairs; broader aggregate kinds and a generic registry remain open. See [CH042_APPROX_DISTINCT_STATE.md](CH042_APPROX_DISTINCT_STATE.md) and [CHU43_TDIGEST_STATE.md](CHU43_TDIGEST_STATE.md). | Exact merge, schema/version rejection, bounded state size, and wider wire-size coverage. |
| CH-U04 | External `DISTINCT` spill | Adopted for direct `EXTERNAL('name')` sources when the resolver implements `ExternalStreamSourceResolver`; materialized resolvers retain the established fallback. | Exact type/NULL semantics, duplicate elimination, cleanup, and memory ceiling. See [CHU04_EXTERNAL_DISTINCT_SPILL.md](CHU04_EXTERNAL_DISTINCT_SPILL.md). |
| CH-U05 | External window-function state | Adopted for direct `EXTERNAL('name')` sources with `ExternalStreamSourceResolver` for a bounded subset: unpartitioned, unordered running windows and fixed-offset `LEAD`; generic partitioned/order-dependent window spill remains open. | Frame correctness, stable ordering, cancellation, and temporary-file limits. See [CHU05_EXTERNAL_WINDOW_STREAM.md](CHU05_EXTERNAL_WINDOW_STREAM.md). |
| CH-U06 | Persistent lightweight delete bitmap | Typed-table patch parts now use a compact in-memory row-existence mask; persistent stored-part delete bitmaps remain open. | Read correctness, update/delete interaction, compaction, and retained-byte crossover. |
| CH-U07 | Mutation wait/status lifecycle | Adopted for asynchronous journal writes: submissions expose a durable journal sequence, committed/rejected/failed state, infrastructure errors, and restart-safe `MutationStatus`; `system.mutations` adds `mutation_id` and `progress` without values. | Live failure handles are process-local, committed history is journal-backed, and authorization remains the caller/server boundary. See [CHU07_MUTATION_LIFECYCLE.md](CHU07_MUTATION_LIFECYCLE.md). |
| CH-U08 | Automatic SQL result-cache wiring | Adopted for direct `HatTrie` materialized SQL entry points through opt-in `ConfigureSQLResultCache`; source-version validation uses the conservative trie mutation epoch and explicit caches remain supported. Streaming/page APIs remain uncached. | Safe default-off behavior, dependency invalidation, parameter isolation, and tiny-versus-expensive workload benchmark. See [CHU08_AUTOMATIC_SQL_RESULT_CACHE.md](CHU08_AUTOMATIC_SQL_RESULT_CACHE.md). |
| CH-U09 | Persisted query-result cache | Adopted as an explicit opt-in warm-start cache: `Persist`/`Restore` retain only versioned SQL entries, validate exact keys and source versions, checksum the bounded binary file, and atomically publish `0600` snapshots. | Persistence is not automatic or a data backup; unsupported values fail closed, sensitive query/result data needs protected storage, and restore has a measured one-time CPU/heap cost. See [CHU09_PERSISTED_SQL_RESULT_CACHE.md](CHU09_PERSISTED_SQL_RESULT_CACHE.md). |
| CH-U10 | Feedback-driven projection selection | Adopted as an explicit opt-in ranking aid: `SQLProjectionAdvisor` records bounded successful slow-query latency totals, and `CostRecommendations` ranks which caller-managed projections should be created or refreshed first. Existing count-ranked recommendations and SQL planner defaults remain unchanged. | The selector is advisory rather than automatic; it retains only caller IDs and `CACHE` dependencies, and its explicit ranking call has a measured one-time allocation cost. See [CHU10_FEEDBACK_PROJECTION_SELECTION.md](CHU10_FEEDBACK_PROJECTION_SELECTION.md). |
| CH-U11 | Automatic data-skipping-index selection | Adopted the bounded advisory selection component: `SQLIndexAdvisor.SkipIndexRecommendations` ranks supported JSON path equality candidates by observed workload cost. Creation and rebuild scheduling remain explicit operator actions. | False-negative proof, rebuild scheduling, and memory/CPU budget were verified for the existing skip index; automatic schema mutation remains intentionally off. See [CHU11_AUTOMATIC_DATA_SKIPPING_INDEX_SELECTION.md](CHU11_AUTOMATIC_DATA_SKIPPING_INDEX_SELECTION.md). |
| CH-U12 | Background skip-index rebuild queue | Adopted as the importable, opt-in `hatSql.SQLIndexRebuildQueue`: bounded pending work, priority/FIFO ordering, cancellation, progress, and bounded terminal history. Automatic index selection, retry policy, disk quota, and endpoint/operator integration remain caller-owned. | Concurrent writes, retry behavior, disk quota, and operator visibility. See [CHU12_BACKGROUND_INDEX_REBUILD_QUEUE.md](CHU12_BACKGROUND_INDEX_REBUILD_QUEUE.md). |
| CH-U13 | Phrase-aware inverted index | Opt-in exact contiguous phrase matching now exists beside token Bloom filtering; ranked token postings remain open. | Unicode/token semantics, update cost, memory crossover, bounded ranking, and explicit memory opt-in. |
| CH-U14 | Join-derived runtime filter propagation | Runtime Bloom filtering exists only in selected paths, not as a general planner/executor filter exchange. | Exact fallback, filter lifetime, false-positive CPU cost, and cancellation. |
| CH-U15 | Vectorized decimal kernels | Adopted as portable fixed-width word-parallel decimal kernels: Decimal128/256 comparison, add/subtract overflow detection, packed batch filtering, and existing RowBinary statistics pruning use caller-owned storage with no valid-path allocation. | Same-scale coefficient arithmetic only; scale alignment, rounding, multiplication, division, and architecture-specific SIMD remain outside this kernel. See [CHU15_VECTORIZED_DECIMAL_KERNELS.md](CHU15_VECTORIZED_DECIMAL_KERNELS.md). |
| CH-U16 | Adaptive low-cardinality dictionaries | Partially adopted by `TypedTableColumn.DictionaryAdaptive`: a bounded first-batch probe admits low-cardinality string columns into the existing dictionary storage and rejects high-cardinality columns without ongoing probe state; runtime demotion/eviction and automatic schema-wide selection remain open. | Deterministic encoding, update churn, memory threshold, and decode cost. See [CHU16_ADAPTIVE_LOW_CARDINALITY.md](CHU16_ADAPTIVE_LOW_CARDINALITY.md) and [BENCHMARK.md](BENCHMARK.md#chu16-adaptive-low-cardinality-string-storage). |
| CH-U17 | Compressed `IN`-set representation | Adopted for dense safe-range integer literal lists: prepared expressions use a bounded `uint64` bitmap plus compact exact fallback values; sparse, mixed, dynamic, and NULL-containing lists retain the existing paths. | Exact type/NULL behavior and build threshold were verified; five-run lookup is 3.67x faster with 1.97x lower retained representation bytes, while preparation is 1.52x slower and remains a reuse tradeoff. See [CHU17_DENSE_INTEGER_IN.md](CHU17_DENSE_INTEGER_IN.md). |
| CH-U18 | Composite primary-mark pruning | Adopted as an opt-in flat tuple-mark path for lexicographically ordered numeric fields, with leading-prefix equality/range pruning and safe fallback for incomplete or unsorted batches. | NULL/non-numeric values are conservative fallbacks; maximum tuple width is eight fields. See [CHU18_COMPOSITE_PRIMARY_MARK_PRUNING.md](CHU18_COMPOSITE_PRIMARY_MARK_PRUNING.md) and [BENCHMARK.md](BENCHMARK.md#ch-u18-composite-primary-mark-pruning). |
| CH-U19 | General `PREWHERE` syntax | Adopted: public `PREWHERE` parsing, parameter binding, streaming/fallback execution, late materialization, and `EXPLAIN` output are implemented. | Broader specialized-resolver pushdown remains bounded by existing execution contracts. |
| CH-U20 | Array/JSON late materialization | Partially adopted by CH-031: an opt-in columnar resolver can materialize reusable typed scalar JSON paths for narrow single-source queries; generic array paths, complex JSON values, and automatic persistent admission remain open. | Exact row alignment, malformed-value behavior, broader query coverage, and allocation benchmark. See [CH031_TYPED_JSON_SUBCOLUMNS.md](CH031_TYPED_JSON_SUBCOLUMNS.md). |
| CH-U21 | Streaming `JSONEachRow` and CSV ingestion | Adopted as bounded reader/callback APIs for `ExternalTables`: CSV and JSONEachRow consume one record at a time, enforce row/byte/record limits, and replace snapshots atomically. | The legacy byte-slice methods remain compatible; JSON object decoding still allocates one row map per record. See [CHU21_STREAMING_TEXT_INGESTION.md](CHU21_STREAMING_TEXT_INGESTION.md) and [BENCHMARK.md](BENCHMARK.md#ch-u21-streaming-text-ingestion). |
| CH-U22 | Direct columnar append ingestion | Partially adopted as the importable `hatSql.TypedTable.AppendColumnar` primitive. It validates complete scalar batches before mutation and appends column-wise; SQL `INSERT` routing and server-level transaction integration remain caller-owned. | Schema/type checks, atomic failure, NULLs, and append throughput are covered in [CHU22_DIRECT_COLUMNAR_APPEND.md](CHU22_DIRECT_COLUMNAR_APPEND.md) and [BENCHMARK.md](BENCHMARK.md#ch-u22-direct-columnar-append). |
| CH-U23 | Async-insert queue status and flush API | Adopted as the importable bounded `AsyncInsertQueueRegistry`, `AsyncInsertBuffer.Stats`, and authenticated monitoring status/flush routes. Registration and lifecycle remain caller-owned, and the feature is off by default. | It is intentionally not a cross-process queue or automatic daemon. See [CHU23_ASYNC_INSERT_QUEUE.md](CHU23_ASYNC_INSERT_QUEUE.md). |
| CH-U24 | Per-table memory budgets | Partially adopted by `TypedTableSchema.MemoryBudget`: bounded logical row admission covers `Upsert`, `AppendColumnar`, patch-retained deletes, and physical patch compaction. Go map capacity, indexes, query working memory, and cluster-wide attribution remain outside the estimate. See [CHU24_TYPED_TABLE_MEMORY_BUDGET.md](CHU24_TYPED_TABLE_MEMORY_BUDGET.md). | No false rejection, nested query attribution, and low-overhead counters. |
| CH-U25 | Per-query spill disk quotas | Adopted as opt-in `SQLQueryOptions.MaxQuerySpillBytes`: one synchronized live-byte ledger is shared by sort, set, GROUP BY, and hash-join spill files, with failure/cancel cleanup and concurrent reservation tests. Process-crash orphan policy remains deployment-owned. | Startup orphan cleanup, exact filesystem accounting, and broader parallel spill scheduling. See [CHU25_QUERY_SPILL_QUOTA.md](CHU25_QUERY_SPILL_QUOTA.md) and [BENCHMARK.md](BENCHMARK.md#chu25-query-wide-spill-quota). |
| CH-U26 | Operator memory profiles | Partially adopted by opt-in `SQLQueryProfiler.RecordMemory`: bounded per-query/operator aggregation reports saturating allocated bytes plus peak and maximum retained bytes, with deterministic snapshots and counters. Measurements remain caller-supplied; process-wide heap capture and automatic executor wiring are intentionally outside this layer. See [CHU26_OPERATOR_MEMORY_PROFILES.md](CHU26_OPERATOR_MEMORY_PROFILES.md). | Low sampling overhead, privacy, deterministic reports, and executor integration. |
| CH-U27 | Priority merge scheduler | Adopted as opt-in `hatStorage.CompactionScheduler.ScheduleWithPriority`: higher-priority queued work runs first, ties remain deterministic, duplicates coalesce, and failed tasks retain priority on retry. The default `Schedule` path remains priority-free. See [CHU27_PRIORITY_MERGE_SCHEDULER.md](CHU27_PRIORITY_MERGE_SCHEDULER.md). | Starvation prevention, stable scheduling, and write amplification. |
| CH-U28 | Disk-I/O merge throttling | Partially adopted as opt-in estimated-byte pacing in `hatStorage.CompactionScheduler`: `MaxIOBytesPerSecond`, `ScheduleWithIO`, context cancellation, retry-safe requeue, and wait counters are implemented. It deliberately does not read OS disk counters or automatically measure foreground latency; callers calibrate the rate. See [CHU28_DISK_IO_THROTTLING.md](CHU28_DISK_IO_THROTTLING.md). | Portable metrics, no deadlock, automatic bandwidth calibration, and foreground tail-latency feedback. |
| CH-U29 | TTL tier movement | Partially adopted by age-based `StorageTierPolicy`, deterministic `PlanStorageTierMoves`, and cancellable `ExecuteStorageTierMoves` in [`storage_tier.go`](hat/hatStorage/storage_tier.go) and [`ch015_storage_tier_move.go`](hat/hatStorage/ch015_storage_tier_move.go); integration with namespace TTL/lifecycle metadata remains open. | Atomic placement metadata, recovery, and movement bandwidth. |
| CH-U30 | Remote-part prefetch cache | Partially adopted by bounded `RemotePartCache` and `Prefetch` in [`remote_part_cache.go`](hat/hatStorage/remote_part_cache.go): byte/entry limits, priority, pinning, and single-flight loading exist; column-aware remote read policy remains open. | Cache admission, eviction, integrity, and read-amplification measurement. |
| CH-U31 | Multipart remote-part upload | Adopted as opt-in `UploadRemotePartMultipart`: persisted upload state, SHA-256 object/part checksums, skip/resume, per-part persistence, explicit abort, and safe state validation. Store-specific multipart/network/auth implementation and automatic orphan cleanup remain caller-owned. See [CHU31_MULTIPART_REMOTE_UPLOAD.md](CHU31_MULTIPART_REMOTE_UPLOAD.md). | Retry safety, orphan cleanup, and bandwidth/CPU accounting. |
| CH-U32 | Remote-part garbage collection | Adopted as opt-in `PlanRemotePartGarbageCollection` plus `ExecuteRemotePartGarbageCollection`: caller-supplied manifest reachability, 24-hour age retention, bounded deterministic plans, explicit sequential deletion, cancellation, and partial progress are implemented. Listing, manifest reads, authorization, retries, and orphan-upload policy remain caller-owned. See [CHU32_REMOTE_PART_GC.md](CHU32_REMOTE_PART_GC.md). | Reachability safety, dry-run plan, retention, and operator authorization. |
| CH-U33 | Quorum part publication | Adopted as opt-in `NewRemotePartPublicationProposal` plus `PublishRemotePartAfterQuorum`: exact reference and manifest fingerprints, deterministic strict-majority votes, fencing-token checks, and post-quorum store publication. See [CHU33_REMOTE_PART_PUBLICATION.md](CHU33_REMOTE_PART_PUBLICATION.md). | Fencing, unavailable peers, retry ordering, and latency. |
| CH-U34 | Idempotent mutation retries | Adopted as opt-in `mutation_id` support on `POST /api/sql`, backed by the command journal and shared with `ExecuteSQLMutationIdempotent`; same-token retries are replay-safe, conflicts fail closed, and restart replay restores the response. Capacity eviction bounds the protection window, and unsupported SQL result semantics remain rejected. See [CHU34_SQL_MUTATION_IDEMPOTENCY.md](CHU34_SQL_MUTATION_IDEMPOTENCY.md). | Exact-once result semantics, conflict behavior, retention, and HTTP/RBAC coverage. |
| CH-U35 | `OPTIMIZE`/merge-control command | Partially adopted as importable opt-in `hatStorage.CompactionController`: bounded target coalescing, job IDs/status, retry state, context cancellation, priority, and I/O estimates are available; HTTP/SQL command wiring and backend-specific target catalogs remain caller-owned. See [CHU35_OPTIMIZE_CONTROL.md](CHU35_OPTIMIZE_CONTROL.md). | No unbounded memory use, cancellation, write throttling, and safe operator integration. |
| CH-U36 | Complete `system.parts` catalog | Adopted as opt-in `SQLSystemPartProvider`: bounded, deterministic SQL rows expose part layout, checksum, key range, lifecycle, and retention metadata without paths or values; the legacy trie count view remains unchanged. | Bounded output, secret-free fields, and schema stability are covered by focused validation and provider-error tests. See [CHU36_SYSTEM_PARTS_CATALOG.md](CHU36_SYSTEM_PARTS_CATALOG.md) and [BENCHMARK.md](BENCHMARK.md#ch-u36-stable-sql-system-parts-catalog). |
| CH-U37 | Complete `system.mutations` catalog | Adopted as opt-in `SQLSystemMutationProvider`: bounded deterministic rows expose stable IDs, sequences, lifecycle, affected parts, redacted errors, and UTC timestamps; the legacy journal tail remains unchanged. | Restart retention, bounded cardinality, and redaction are covered by focused provider tests. See [CHU37_SYSTEM_MUTATIONS_CATALOG.md](CHU37_SYSTEM_MUTATIONS_CATALOG.md) and [BENCHMARK.md](BENCHMARK.md#ch-u37-stable-sql-system-mutations-catalog). |
| CH-U38 | Sampled query-log export | Implemented as opt-in `SQLQueryLogOptions.SampleRate`/`SampleSeed` with deterministic bounded sampling counters; `0` and `1` retain all, and dropped valid entries skip rotation and writes. | Stable sampling, literal privacy, and bounded CPU/storage are covered by `make test-chu38` and `BENCHMARK.md#ch-u38-sampled-query-log-export`. |
| CH-U39 | Workload admission priorities | Implemented as opt-in `SQLWorkloadAdmission` with bounded pending work, priority and weighted class selection, starvation protection, cancellation, close, stats, and panic-safe release. See [CHU39_WORKLOAD_ADMISSION.md](CHU39_WORKLOAD_ADMISSION.md). | Fairness, cancellation, and default zero overhead. |
| CH-U40 | Dependency-aware result invalidation | Cached SQL results do not carry a first-class source/partition dependency set for selective invalidation. | Implemented as opt-in dependency tracking with version-safe and explicit invalidation paths; persistence and mutation-race tests plus benchmark added. |
| CH-U41 | Schema-versioned plan cache | Implemented opt-in bounded binary persistence for prepared SQL templates, exact schema-version admission, checksum validation, atomic writes, and parameter-value isolation. See [CHU41_SCHEMA_VERSIONED_PLAN_CACHE.md](CHU41_SCHEMA_VERSIONED_PLAN_CACHE.md). | Safe invalidation, parameter isolation, and memory cap. |
| CH-U42 | SQL approximate distinct aggregate | Implemented `APPROX_COUNT_DISTINCT_STATE` and `APPROX_COUNT_DISTINCT_MERGE` on the streaming HyperLogLog path, with `NULL`, malformed-state, precision-mismatch, and empty-merge semantics covered. See [CH042_APPROX_DISTINCT_STATE.md](CH042_APPROX_DISTINCT_STATE.md) and [BENCHMARK.md#ch-042-mergeable-approximate-distinct-states](BENCHMARK.md#ch-042-mergeable-approximate-distinct-states). | Error bounds, state-version compatibility, and wider SQL planner integration. |
| CH-U43 | SQL t-digest quantiles | Implemented `APPROX_TDIGEST_PERCENTILE_STATE(value[, compression])` and `APPROX_TDIGEST_PERCENTILE_MERGE(state, quantile)` on the existing bounded t-digest codec, with empty, malformed, finite-value, and compression-mismatch semantics. See [CHU43_TDIGEST_STATE.md](CHU43_TDIGEST_STATE.md) and [BENCHMARK.md#ch-u43-mergeable-sql-t-digest-percentile-states](BENCHMARK.md#ch-u43-mergeable-sql-t-digest-percentile-states). | Rank-error contract, retractions, and wider planner integration. |
| CH-U44 | SQL aggregate combinators | Implemented filtered built-in state and merge forms including `*_STATE_IF`, `*_MERGE_IF`, arg-extreme variants, strict arity diagnostics, and composition with `FILTER (WHERE ...)`. See [CHU44_SQL_AGGREGATE_COMBINATORS.md](CHU44_SQL_AGGREGATE_COMBINATORS.md) and [BENCHMARK.md#ch-u44-sql-aggregate-combinators](BENCHMARK.md#ch-u44-sql-aggregate-combinators). | Wider planner integration and SQL parser names for custom registered combinators. |
| CH-U45 | `WITH TIES` limit semantics | Ordered SQL limits do not expose tie-preserving output around the boundary. | NULL/collation behavior, stable ties, and streaming memory. |
| CH-U46 | `ASOF JOIN` execution | SQL does not provide an ordered nearest-prior temporal join operator. | Timestamp ordering, duplicate ties, NULLs, and index usage. |
| CH-U47 | Dictionary lookup functions | There is no SQL dictionary object/function contract with refresh, miss, and version semantics. | Bounded lookup latency, refresh atomicity, and secret handling. |
| CH-U48 | Ranked full-text search | Adopted as the opt-in `NewRankedTokenPostingsIndex` and bounded `MatchRanked`/`MatchRankedInto` APIs with BM25-like IDF, term-frequency, document-length scoring, and deterministic top-K output. Plain token postings remain unchanged and default-off for ranking. | Unicode/tokenization follows the existing postings index; ranked metadata increases build/update memory and CPU. See [CHU48_RANKED_FULL_TEXT.md](CHU48_RANKED_FULL_TEXT.md) and [BENCHMARK.md](BENCHMARK.md#chu48-ranked-full-text). |
| CH-U49 | Skip-index explain diagnostics | Implemented for direct equality probes over selected `CACHE` JSON-path skip indexes; other index families and compound predicates remain open. | No query-result changes, bounded metrics, and low default overhead. |
| CH-U50 | Part/WAL-consistent backup manifest | Implemented: snapshot, Pebble checkpoint, and incremental manifests bind immutable payload files and the exact part/journal boundaries with per-file SHA-256 values and a deterministic digest; legacy manifests remain readable. | Concurrent writes, restore rehearsal, crash recovery, and bandwidth. |

## Materialize

| ID | Candidate | Current gap | Adoption gate |
|---|---|---|---|
| M-U01 | Durable connector lifecycle state | Adopted as an explicit `hatPipeline.ConnectorRegistry` checkpoint: bounded status/history is encoded as deterministic, CRC32C-protected `HCS1` bytes and restored with caller-supplied fresh connector implementations. Restore never invokes callbacks and remains caller-owned for restart reconciliation. | Crash/restart metadata recovery, version validation, bounded persistence, and no query-path overhead are covered. See [MU01_DURABLE_CONNECTOR_STATE.md](MU01_DURABLE_CONNECTOR_STATE.md). |
| M-U02 | Connector schema evolution | Adopted: deterministic catalog fingerprints and compatibility plans accept safe nullable additions, project mixed-version rows, and publish atomic generations with compare-and-swap rollback. | Required-field/type narrowing rejection, index dependency checks, exact numeric widening, concurrent snapshots, and rollback are covered. See [MU02_CONNECTOR_SCHEMA_EVOLUTION.md](MU02_CONNECTOR_SCHEMA_EVOLUTION.md). |
| M-U03 | External source snapshot ingestion | Adopted: provider-neutral authenticated snapshot paging accepts bounded Kafka/PostgreSQL/CDC adapter data, validates offsets, atomically publishes a SQL resolver snapshot, and recovers from checkpoints without upstream access. | Page/row backpressure, identity and duplicate-offset rejection, cancellation, deep-copy isolation, failed-commit rollback, and recovery are covered. See [MU03_EXTERNAL_SNAPSHOT_INGESTION.md](MU03_EXTERNAL_SNAPSHOT_INGESTION.md). |
| M-U04 | Multi-source snapshot coordinator | hatSql.SQLMultiSourceSnapshotCoordinator now captures bounded authenticated source snapshots concurrently, commits one complete checkpoint, publishes an atomic view, and restores without re-authentication while retaining each source snapshot ID and offsets. | Provider-independent storage, source failure atomicity, exact version retention, and bounded memory. |
| M-U05 | Arrangement-only recovery mode | Persisted state can hydrate selected structures, but no public recovery mode restores all maintained arrangements without rereading a source. | Checkpoint compatibility, missing history, and source-version validation. |
| M-U06 | Arbitrary differential window frames | `hatSql.DifferentialWindow` now supports signed weighted updates, retractions, late rows, partitioned `ROWS`/`RANGE` frames, and atomic correction output. | Full SQL frame clauses beyond bounded numeric offsets, durable spill, and frontier-driven eviction. |
| M-U07 | Declarative late-data dataflow policy | `hatSql.DifferentialDataflow` now couples allowed lateness, accept/drop/reject correction modes, monotonic manual or batch-max frontiers, atomic sink delivery, and operator statistics. | Durable policy checkpoints, multi-input frontier joins, and automatic retained-state compaction. |
| M-U08 | Temporal join state compaction | `hatSql.DifferentialTemporalJoin.Compact` now evicts rows only when same-side sealing and counterpart match bounds are proven, rebuilds group indexes, and reports retained/removed state. | Durable frontier checkpoints, multi-input frontier coordination, and spill-backed historical retention. |
| M-U09 | Durable frontier snapshots | `hatPipeline.FrontierRegistry` now provides bounded file-backed `SaveDurableSnapshot`/`RestoreDurableSnapshot` checkpoints with private atomic replacement, file and directory syncs, missing/corrupt distinction, and caller-defined `FrontierSnapshotStore` support. | WAL/source-offset transaction ordering and remote object-store implementations remain caller-owned. |
| M-U10 | Adaptive logical compaction | `hatSql.DifferentialTemporalJoin` now offers opt-in update/estimated-bytes/frontier-age recommendations and `CompactIfNeeded`, preserving exact frontier safety and cancellation-atomic collection. | Automatic background scheduling and allocator-exact memory measurement remain caller-owned. |
| M-U11 | Arrangement reuse advisor | Implemented in [MU11_ARRANGEMENT_REUSE_ADVISOR.md](MU11_ARRANGEMENT_REUSE_ADVISOR.md): deterministic exact aggregate/join reuse advice, stale hydration classification, and saturating memory estimates. | The caller still owns acquisition, hydration, compacted-changefeed fallback, and workload-specific cost calibration. |
| M-U12 | Arrangement keys in `EXPLAIN` | Implemented in [MU12_EXPLAIN_ARRANGEMENTS.md](MU12_EXPLAIN_ARRANGEMENTS.md): bounded deterministic enriched explain JSON and DOT output with arrangement keys, actions, staleness, cardinality, and memory estimates. | The caller supplies catalog-derived annotations and maps them to exact plan step positions; default EXPLAIN remains unchanged. |
| M-U13 | Dataflow cardinality estimates | Adopted: `EXPLAIN` uses available source and JSON-index statistics for bounded scan, filter, join, and aggregate estimates; `EXPLAIN ANALYZE` reports observed counts and estimate error, while unsupported metadata stays unknown. | Uncertainty reporting, privacy, and no plan regression are covered. See [MU013_SQL_CARDINALITY_ESTIMATES.md](MU013_SQL_CARDINALITY_ESTIMATES.md) and [BENCHMARK.md](BENCHMARK.md#mu-013-sql-cardinality-estimates). |
| M-U14 | Full object dependency catalog | Adopted: versioned `information_schema.objects` and `information_schema.dependencies` expose source/view/index/sink metadata, with deterministic bounded cycle-safe closure traversal. | Bounded traversal, cycle handling, and schema stability are covered. See [MU014_DEPENDENCY_CATALOG.md](MU014_DEPENDENCY_CATALOG.md) and [BENCHMARK.md](BENCHMARK.md#mu-014-object-dependency-catalog). |
| M-U15 | SQL source-status catalog | Adopted: `information_schema.source_status` and `SHOW SOURCE STATUS` expose versioned lifecycle, readiness, frontier, lag, and bounded error-code status with a one-call snapshot contract. | Label cardinality, redaction, and snapshot consistency are covered. See [MU015_SOURCE_STATUS_CATALOG.md](MU015_SOURCE_STATUS_CATALOG.md) and [BENCHMARK.md](BENCHMARK.md#mu-015-sql-source-status-catalog). |
| M-U16 | Transactional view create/replace | Adopted: `SQLSession.ApplyViewChanges` stages and validates dependent session-local views, publishes them under one catalog version boundary, and supports `CREATE OR REPLACE VIEW`; durable catalog and query isolation remain caller-owned. | Rollback, concurrent readers, and dependency validation are covered. See [MU016_TRANSACTIONAL_VIEW_DDL.md](MU016_TRANSACTIONAL_VIEW_DDL.md) and [BENCHMARK.md](BENCHMARK.md#mu-016-transactional-view-ddl). |
| M-U17 | Backfill/frontier handoff | Adopted: `IncrementalProjectionRunner.BackfillAtFrontier` waits on all partitions, refreshes from one frontier-bound snapshot, durably checkpoints the boundary, and lets the caller resume at `frontier+1`. | No duplicate/missing updates, cancellation, and cutover atomicity are covered. See [MU017_FRONTIER_BACKFILL.md](MU017_FRONTIER_BACKFILL.md) and [BENCHMARK.md](BENCHMARK.md#mu-017-frontier-safe-backfill-handoff). |
| M-U18 | Exactly-once sink checkpoints | Adopted: `hatSql.SQLSinkExactlyOnceLedger` passes explicit idempotency keys to external delivery, advances monotone sink frontiers, bounds replay history, and restores through an optional atomic checkpoint store. | Crash/retry behavior, duplicate suppression, and bounded history are covered. See [MU018_EXACTLY_ONCE_SINK.md](MU018_EXACTLY_ONCE_SINK.md) and [BENCHMARK.md](BENCHMARK.md#mu-018-exactly-once-sink-checkpoints). |
| M-U19 | Source transaction grouping across relations | Adopted: `hatSql.SQLSourceTransactionEnvelope` and its coordinator group relation membership and source offsets under one idempotent callback, retain commit markers, and provide envelope-preserving snapshot/restore. | Atomic callback boundaries, malformed input, conflict detection, and recovery are covered. See [MU019_SOURCE_TRANSACTION_ENVELOPE.md](MU019_SOURCE_TRANSACTION_ENVELOPE.md) and [BENCHMARK.md](BENCHMARK.md#mu-019-source-transaction-envelopes). |
| M-U20 | Secret/connection resources | Adopted as the opt-in `hatAuth.ResourceRegistry`: bounded named secrets and connections, scoped owners/readers, redacted metadata, exact-version rotation, and metadata-only snapshots. Values remain caller-managed through an external secret manager. | Redaction, reload, authorization, and no credential leakage are covered. See [MU020_SECRET_CONNECTION_RESOURCES.md](MU020_SECRET_CONNECTION_RESOURCES.md) and [BENCHMARK.md](BENCHMARK.md#mu-020-secret-and-connection-resources). |
| M-U21 | Role and namespace hierarchy | Adopted as the opt-in `hatAuth.RoleCatalog`: bounded owned roles and namespaces, parent-role inheritance, scoped grants, principal membership, exact-version mutation fencing, and deterministic snapshot/restore. Legacy `hatAuth.Policy` behavior is unchanged. | Default deny, migration compatibility, policy invalidation, ownership, and cycle validation are covered. See [MU021_ROLE_NAMESPACE_CATALOG.md](MU021_ROLE_NAMESPACE_CATALOG.md) and [BENCHMARK.md](BENCHMARK.md#mu-021-role-and-namespace-catalog). |
| M-U22 | Connector transaction retry journal | Adopted as the opt-in `hatSql.SQLConnectorTransactionRetryJournal`: bounded intent/outcome records, fenced retry attempts, terminal-only eviction, deterministic snapshot/restore, and CRC-protected binary checkpoints. | Idempotent replay, ambiguous failure handling, and retention are covered. See [MU022_CONNECTOR_TRANSACTION_RETRY_JOURNAL.md](MU022_CONNECTOR_TRANSACTION_RETRY_JOURNAL.md) and [BENCHMARK.md](BENCHMARK.md#mu-022-connector-transaction-retry-journal). |
| M-U23 | Per-cluster query admission | Adopted as the opt-in `hatSql.SQLClusterAdmission`: independent serving/maintenance CPU and optional memory pools, bounded queues, context cancellation, idempotent leases, and deterministic usage snapshots. | Fairness, starvation prevention, cancellation, and resource bounds are covered. See [MU023_CLUSTER_QUERY_ADMISSION.md](MU023_CLUSTER_QUERY_ADMISSION.md) and [BENCHMARK.md](BENCHMARK.md#mu-023-cluster-query-admission). |
| M-U24 | Workload classes and priorities | Adopted as shared `SQLClusterAdmissionRequest.WorkloadClass` and bounded `Priority` metadata for source, compute, sink, and ad-hoc work, with a legacy FIFO fastpath and bounded aging to prevent starvation. | Backward-compatible defaults, priority ordering, cancellation bookkeeping, and starvation tests are covered. See [MU024_WORKLOAD_PRIORITIES.md](MU024_WORKLOAD_PRIORITIES.md) and [BENCHMARK.md](BENCHMARK.md#mu-024-workload-classes-and-priorities). |
| M-U25 | Durable redacted query history | Already adopted as the opt-in `SQLQueryLog`: terminal manager status is stored as privacy-safe NDJSON with restrictive permissions, restart-readable history, explicit sync, size/age rotation, bounded archives, and deterministic sampling. | Literal/secret redaction, retention, restart recovery, sampling, and disk-failure behavior are covered by the existing persistent-log and sampling tests. See [PERSISTENT_QUERY_LOG.md](PERSISTENT_QUERY_LOG.md), [SQL_QUERY_LOG.md](SQL_QUERY_LOG.md), and [BENCHMARK.md](BENCHMARK.md#ch-031-persistent-sql-query-log). |
| M-U26 | Unified source/compute/sink metrics catalog | Adopted as the opt-in `hatSql.SQLDataflowMetricsCatalog`: bounded source/compute/sink objects, atomic shape replacement, allocation-free registered-metric updates, deterministic SQL-shaped rows, and independent snapshots. | Cardinality bounds, scrape overhead, stable schema, validation, and concurrent access are covered. See [MU026_UNIFIED_DATAFLOW_METRICS.md](MU026_UNIFIED_DATAFLOW_METRICS.md) and [BENCHMARK.md](BENCHMARK.md#mu-026-unified-dataflow-metrics). |
| M-U27 | Logical publication/subscription | Partially adopted as opt-in `hatSql.SQLPublication`: fixed-schema contiguous logical batches, bounded replay, checksummed consumer checkpoints, and explicit slow-consumer handling. Durable history, fresh snapshots, connector wiring, and exactly-once sink transactions remain caller-owned. | Schema validation, contiguous revisions, checkpoint expiry/monotonicity, terminal completion, backpressure, and binary checksum validation are covered. See [MU027_LOGICAL_PUBLICATION.md](MU027_LOGICAL_PUBLICATION.md) and [BENCHMARK.md](BENCHMARK.md#mu-027-logical-sql-publications). |
| M-U28 | General monotonicity inference | Partially adopted as the opt-in public `hatSql.AnalyzeSQLExpressionMonotonicity` proof helper for SQL expressions. Planner-wide joins, filters, aggregates, schema-aware types, and automatic fast-path selection remain out of scope. | Conservative fallback, NULL reporting, parser diagnostics, retractions, and proof tests are covered. See [MU028_MONOTONICITY.md](MU028_MONOTONICITY.md) and [BENCHMARK.md](BENCHMARK.md#mu-028-sql-monotonicity-inference). |
| M-U29 | General recursive dataflow maintenance | Adopted as an opt-in mutable recursive reachability API; automatic recursive SQL planner integration remains deferred. | Cycles, termination, retractions, localized recomputation, and memory bounds. See [INCREMENTAL_RECURSIVE_REACHABILITY.md](INCREMENTAL_RECURSIVE_REACHABILITY.md). |
| M-U30 | Arrangement-native top-N retractions | Partially adopted as an opt-in exact weighted incremental Top-K operator; generic arrangement and planner integration remain deferred. | Duplicate ties, deletes, update cost, and retained-row ownership. See [MZ037_INCREMENTAL_TOP_K.md](MZ037_INCREMENTAL_TOP_K.md). |
| M-U31 | User-defined retractable aggregates | Partially adopted as opt-in retractable and binary-serializable aggregate capabilities; rollback, panic isolation, and automatic planner wiring remain deferred. | Capability errors, legacy compatibility, state snapshots, and constructor overhead. See [MU031_RETRACTABLE_AGGREGATES.md](MU031_RETRACTABLE_AGGREGATES.md) and [BENCHMARK.md](BENCHMARK.md#mu-031-retractable-aggregate-capabilities). |
| M-U32 | UDF purity classification | Partially adopted as explicit validated UDF capability metadata and defensive catalog lookup; automatic planner use remains deferred. | Conservative defaults, metadata validation, persistence, error behavior, and authorization. See [MU032_UDF_CAPABILITIES.md](MU032_UDF_CAPABILITIES.md) and [BENCHMARK.md](BENCHMARK.md#mu-032-udf-capability-classification). |
| M-U33 | As-of retention policy | `hatPipeline.FrontierRetentionRegistry` now tracks bounded per-frontier as-of leases and exposes the safe compaction boundary; storage engines must still consult it and recreate leases after restart. | No premature compaction, bounded storage, and removal. |
| M-U34 | Historical subscription cancellation | Durable subscriptions lack a complete cancellation/checkpoint protocol for historical replay interrupted mid-stream. | No duplicate/missing records and restart recovery. |
| M-U35 | Snapshot blocking semantics | Source snapshotting does not expose a common queryable state that blocks dependents until all required objects are ready. | Per-object progress, cancellation, and read consistency. |
| M-U36 | Hydration progress and admission | Arrangement hydration lacks a first-class progress/status contract that controls query admission. | Restart latency, readiness, and no source reread. |
| M-U37 | Compaction diagnostics by arrangement | Adopted as opt-in bounded `hatStorage.CompactionDiagnostics` with deterministic per-arrangement snapshots, fixed-depth history, and allocation-free recording after registration. | Labels are explicit and bounded; the caller defines compaction debt and owns scheduler/storage wiring. See [MU037_COMPACTION_DIAGNOSTICS.md](MU037_COMPACTION_DIAGNOSTICS.md) and [BENCHMARK.md](BENCHMARK.md#m-u37-per-arrangement-compaction-diagnostics). |
| M-U38 | Persisted immutable data parts | Persistence uses existing storage paths, but there is no Materialize-style immutable part manifest with object-store lifecycle. | Checksums, garbage collection, crash recovery, and read amplification. |
| M-U39 | Partition/order declarations | SQL objects cannot declare expected partition ordering that is exposed to planning and filter pushdown. | No semantic data rewrite, explain visibility, and fallback. |
| M-U40 | Source schema registry integration | CDC/schema changes are not validated against a registry version and compatibility policy. | Version mismatch, rollback, and secret-safe diagnostics. |
| M-U41 | Webhook source idempotency | Adopted as opt-in bounded `hatSql.WebhookEventDeduplicator` with payload-fingerprint conflict detection, TTL expiry, deterministic CRC-protected snapshots, and atomic restore. | The caller owns event application, durable storage, acknowledgement, and webhook wiring; payload bytes are never retained. See [MU041_WEBHOOK_IDEMPOTENCY.md](MU041_WEBHOOK_IDEMPOTENCY.md) and [BENCHMARK.md](BENCHMARK.md#m-u41-webhook-event-idempotency). |
| M-U42 | Connector pause/resume checkpoints | A source cannot be paused and resumed with a durable offset/frontier checkpoint through one public operation. | Crash recovery, stale resumes, and operator audit. |
| M-U43 | Sink backpressure catalog | Sink lag/backpressure is not exposed as a common frontier-aware state that can throttle upstream work. | No deadlock, cancellation, and bounded queues. |
| M-U44 | Cross-dataflow transaction visibility | Adopted as an opt-in publication barrier: `SQLDataflowVisibilityCoordinator` assigns a common logical version to an explicitly prepared batch and rejects mixed or stale observations. | Caller-owned preparation, bounded metadata, and no historical row retention. |
| M-U45 | Incremental join index selection | Join plans do not automatically choose and maintain the smallest compatible arrangement for changing predicates. | Duplicate semantics, update cost, and explainability. |
| M-U46 | Differential export/import | Implemented as bounded HDF1 checkpoint export/import with typed values, CRC32C validation, deterministic ordering, and atomic frontier application. | Version compatibility, type preservation, bounded decode, and atomic import are covered by focused, race, and vet verification. |
| M-U47 | Progress-only subscription frames | Implemented as explicit bounded QPF1 frames carrying subscription id, revision, frontier, completion, version, and CRC32C without row data. | Ordering metadata, corruption handling, and explicit JSON compatibility are covered by focused, race, vet, and benchmark verification. |
| M-U48 | Source connector health remediation | Implemented: opt-in bounded retry, exponential backoff, context cancellation, and quarantine through `StartWithHealthPolicy`. | Avoid retry storms, preserve offsets, and operator override without changing the default lifecycle path. |
| M-U49 | Catalog migration runner | Implemented in [M049_CATALOG_MIGRATION.md](M049_CATALOG_MIGRATION.md): dependency-aware validation/dry-run ordering, serial apply, and reverse rollback callbacks. | The caller still owns SQL locks, durable history, retries, engine-specific compatibility, and mixed-version rollout policy. |
| M-U50 | Durable frontier-based backup | Implemented in [M050_DURABLE_FRONTIER_BACKUP.md](M050_DURABLE_FRONTIER_BACKUP.md): bounded deterministic binary manifest plus restore-order and journal-gap validation for storage, sources, frontiers, and subscriptions. | The caller still owns source barriers, file/checksum verification, journal replay, rehearsal, and durable publication. |

## Tarantool

| ID | Candidate | Current gap | Adoption gate |
|---|---|---|---|
| T-U52 | Per-peer adaptive breaker policy | `hatPeer.ConnectionPool` now supports opt-in bounded cooldown backoff after failed probes and decay after recovery; failure-class-specific thresholds remain caller policy. | Preserve deterministic operator bounds, avoid false opens, keep the default disabled, and validate recovery behavior. |
| T-U02 | Authenticated compact peer daemon integration | `hatPeer` now provides an explicit bounded listener with fixed-size version/feature negotiation, mandatory authorization, optional TLS enforcement, admission limits, handshake deadlines, and clean session shutdown; full cluster membership remains caller-owned. | Complete compatibility evolution, flow-control policy, and head-of-line behavior without enabling a daemon by default. |
| T-U03 | Stored procedure registry | External extension boundaries exist, but no trusted in-process stored function registry exposes stable call semantics. | Authorization, panic isolation, and versioning. |
| T-U04 | Sandboxed stored Lua/runtime functions | There is no resource-limited embedded scripting runtime for stored procedures. | Sandbox escape resistance, CPU/memory limits, and disable-by-default policy. |
| T-U05 | Session transaction settings | Transactions exist, but client/session defaults for isolation, timeout, read-only, and durability are not a unified contract. | Inheritance, reset, and authorization. |
| T-U06 | Replica-wide read-only enforcement | Leader-write enforcement protects selected service paths, but every direct mutation API cannot be locked by a replica read-only state. | No bypass path, internal replication exception, and operator override. |
| T-U07 | Replica lag/RPO status object | `hatReplication` now provides clock-independent underflow-safe per-replica RPO status values and a bounded order-preserving batch builder; integration with live node metrics and failover policy remains caller-owned. | Bounded metrics, clock safety, and failover use. |
| T-U08 | Snapshot plus exact WAL coordinate | `hatCache.CommandJournal` now offers an opt-in online snapshot manifest with exact journal sequence, byte count, SHA-256, and a file verifier; snapshot-plus-WAL join orchestration remains caller-owned. | Concurrent writes, checksums, and restore rehearsal. |
| T-U09 | Snapshot-plus-WAL join bootstrap | Cluster join lacks a standard snapshot transfer, journal catch-up, fencing, and atomic activation workflow. | Duplicate apply, failure cleanup, and stale member fencing. |
| T-U10 | Journal-wide synchronous write quorum | Quorum APIs are selective; no per-write journal contract waits for configured replica acknowledgements. | Unavailable peers, latency, retry ordering, and default off. |
| T-U11 | Per-space conflict policy | `hatReplication.ConflictPolicyRegistry` provides bounded per-space last-write-wins, source-priority, and reject policies with copied configuration and deterministic fallbacks; applying it to a concrete write path remains caller-owned. | Deterministic clocks, auditability, and migration. |
| T-U12 | Automatic failover workflow | Election/fencing primitives exist, but health-triggered promotion with quorum and operator policy is not complete. | Split-brain prevention, false positives, and override. |
| T-U13 | Durable cluster membership | Topology membership is not a durable consensus-backed join/leave record with generations. | Stale membership, backups, and quorum loss. |
| T-U14 | VShard bucket map and migration | Partitioning exists, but automatic bucket ownership and migration with backup semantics are not implemented. | Ownership, migration interruption, and recovery design. |
| T-U15 | Named space catalog | `hatSchema.SpaceCatalog` combines a named versioned source, constraints, and validated hash/tree/R-tree/functional index declarations with bounded clone-safe lookup and listing; execution-layer index construction remains caller-owned. | Field numbering, schema validation, and evolution. |
| T-U16 | Selectable memtx-style row engine | HAT-trie and typed layouts exist, but no per-space predictable in-memory tuple engine is selectable. | Memory overhead, tuple access, and benchmark crossover. |
| T-U17 | Selectable vinyl-style LSM engine | Pebble persistence exists, but no user-selectable table engine exposes explicit LSM read/write tradeoffs. | Compaction, read amplification, backup, and recovery. |
| T-U18 | Explicit volatile cache engine | TTL/cache commands exist, but durability intent is not represented by a separate memory-only engine contract. | Eviction, memory accounting, and operator safety. |
| T-U19 | Durable tuple field-operation journal | Allocation-light generic tuple assignment/add/splice batches exist, but those field operations are not represented by a durable replayable journal record. | Overflow/type recovery, atomic replay, version compatibility, and bounded record size. |
| T-U20 | Online space upgrade | Schema validation exists, but records cannot be converted in the background while compatible reads/writes continue. | Dual-format reads, progress, cutover, and crash recovery. |
| T-U21 | Versioned space migration manager | There is no durable named migration plan with preconditions, progress, mixed-version clients, and rollback. | Resume after crash and dependency validation. |
| T-U22 | Cross-index unique constraints | Hash/ordered/functional indexes exist, but a named space cannot atomically enforce uniqueness across multiple maintained indexes. | Concurrent writes, rollback, and error determinism. |
| T-U23 | Automatic multikey tuple indexes | Array/multikey helpers exist, but generic tuple nested-array expansion is not a maintained space-index contract. | Duplicate elements, delete correctness, and explosion limits. |
| T-U24 | Conditional space indexes | Conditional functional indexes exist, but there is no schema-level conditional index lifecycle with planner metadata. | Predicate determinism, updates, rebuild, and false negatives. |
| T-U25 | R-tree space integration | An R-tree exists, but tuple writes and SQL planning do not automatically maintain/use it as a named-space index. | Update/delete correctness and rebuild cost. |
| T-U26 | Index hints and strategy inspection | Callers cannot request a specific embedded index strategy or inspect why another was selected. | Unsupported-hint errors and explainability. |
| T-U27 | Prefix/config watchers over peer connections | Local key watchers exist, but peer connections cannot watch remote configuration paths/prefixes with reconnect semantics. | Ordering, reconnect gaps, auth, and backpressure. |
| T-U28 | Connection/schema lifecycle triggers | `hatPeer.PeerLifecycleRegistry` now provides bounded connect, disconnect, shutdown, and schema-reload hooks, and `CompactPeerSession` emits opt-in connection terminal events; other daemon, pool, and schema paths still do not publish lifecycle events. | Callback ordering, concurrency, cleanup, and broader integration. |
| T-U29 | Stream transaction API | `hatPeer.CompactPeerStreamEndpoint` now provides bounded begin/call/commit/rollback streams over one compact connection, with ordered per-stream calls and server-side state validation; durable transaction recovery remains handler-owned. | Cancellation, connection loss, transaction recovery, and atomicity. |
| T-U30 | Cooperative fiber scheduler | Pipelines and goroutines exist, but no fiber abstraction provides yield/resume scheduling with measured stack overhead. | Fairness, cancellation, and comparison against goroutines. |
| T-U31 | Fiber channels and conditions | General typed coordination exists, not fiber-aware channels, conditions, semaphores, and wait groups. | Close semantics, deadlock behavior, and memory. |
| T-U32 | Fiber-local storage | Request context exists, but no safe task-local storage contract avoids accidental value leakage across reused workers. | Cleanup, reuse, and race coverage. |
| T-U33 | Function grants and roles | Service authorization exists, but function/space-level grants are not a complete embedded least-privilege model. | Default deny, policy cache invalidation, and audit. |
| T-U34 | Per-space WAL sync policy | Journal group commit exists, but a named space cannot choose synchronous, periodic, or disabled WAL behavior. | Unsafe-default prevention and crash tests. |
| T-U35 | WAL encryption and key rotation | Implemented as opt-in authenticated AES-GCM journal frames with key IDs, mixed legacy compatibility, keyring rotation, and encrypted offline checkpoint installation. | Nonces, corruption recovery, rotation, and secret handling. See [TR008_WAL_ENCRYPTION.md](TR008_WAL_ENCRYPTION.md). |
| T-U36 | Snapshot rotation policy | Adopted as opt-in `BackupRotationPolicy` and `CreateIncrementalBackupRepositoryIfDue`, combining interval/journal-delta triggers with count and approximate unique-byte retention; existing backup and write defaults remain unchanged. See [TU36_SNAPSHOT_ROTATION.md](TU36_SNAPSHOT_ROTATION.md). | Crash consistency and predictable recovery time. |
| T-U37 | Replica applier throttling | Adopted as opt-in `hatReplication.ApplierThrottle`, wired to HTTP journal pulls and gRPC replication streams; nil preserves the legacy path. See [TU37_REPLICA_APPLIER_THROTTLE.md](TU37_REPLICA_APPLIER_THROTTLE.md). | No starvation, lag bounds, and tail latency. |
| T-U38 | Conflict introspection stream | Conflict resolution does not expose a durable redacted stream of conflicted keys, sources, and decisions. | Privacy, bounded retention, and replay diagnostics. |
| T-U39 | Space-level changefeed | Key watchers and SQL subscriptions exist, but no named-space changefeed has schema/version, checkpoint, and backpressure semantics. | Ordering, retractions, reconnect, and checkpoint recovery. |
| T-U40 | Tuple format version negotiation | `hatDataStructure.TupleFormat` now exposes bounded capability advertisements with stable physical-shape fingerprints, deterministic HTF1 encoding, and highest exact common-version selection; the caller still owns the transport handshake and migration policy. | Unknown-field behavior and downgrade safety. |
| T-U41 | Snapshot-consistent iterator cursors | `hatDataStructure.OrderedIndex.SnapshotCursor` now provides an opt-in zero-copy stable view across concurrent mutations, with explicit seek/close/EOF semantics and existing copy-on-write retention; named-space registry wiring remains caller-owned. | Memory bound, invalidation, and repeatable ordering. |
| T-U42 | Cursor `after` pagination contract | `hatDataStructure.CursorTokenCodec` now provides bounded HMAC-authenticated index/schema-bound continuation tokens, and ordered cursors resume after the complete (key, ID) position; transport and SQL endpoint integration remain caller-owned. | Schema/version binding, tamper resistance, and no skipped rows. |
| T-U43 | Per-space memory quotas | `hatStorage.SpaceMemoryQuota` and its registry now provide opt-in named-space byte admission with atomic reserve/release, deterministic snapshots, and default zero integration overhead; callers still declare logical bytes and release them on free. | Accurate attribution, no deadlock, and default zero overhead. |
| T-U44 | Slab fragmentation diagnostics | Runtime heap metrics do not expose allocator classes, fragmentation, and reusable free space in a portable read-only report. | Platform portability and sampling cost. |
| T-U45 | Per-space operation statistics | `hatMetrics.SpaceOperationMetrics` and its bounded registry now provide opt-in named space/index counters for operation families, bytes, hits/misses/errors, and latency totals with an allocation-free handle path; callers still choose integration points and label cardinality. | Label cardinality and allocation-free default path. |
| T-U46 | Index cardinality/hot-key statistics | `hatDataStructure.IndexStats` now provides opt-in fixed-memory HyperLogLog cardinality, exact posting-length counters, and Space-Saving hot-key hashes; index structures remain uninstrumented until callers observe keys/lookups. | Privacy, sampling, and update overhead. |
| T-U47 | Cancellation/deadline propagation to peer calls | `hatPeer.ConnectionPool.DoWithLifecycleContext` now composes caller cancellation/deadlines with pool shutdown for opt-in gRPC, HTTP/2, and compact-protocol handlers; legacy `Do` remains unchanged for zero-allocation callers. | Connection reuse, partial response, and leak tests. |
| T-U48 | Idempotent remote-call retry policy | `hatPeer.RetryPolicy` now provides opt-in method-aware bounded retries with stable idempotency keys, fencing tokens, cancellation-aware exponential backoff/jitter, and observer events; remote handlers still enforce deduplication. | No duplicate mutation, jitter, and observability. |
| T-U49 | Replica-set request hedging | `hatTopology.ExecuteReplicaHedged` now provides opt-in bounded read hedging with delayed fallback, first-success cancellation, deterministic failures, observer events, and a zero-allocation single-candidate fast path. | Tail-latency versus duplicate load and consistency. |
| T-U50 | Cluster-wide configuration watch | `hatTopology.ConfigWatchLog` now provides an authenticated bounded versioned log with replay cursors, context-aware wait/resume, deterministic history-gap errors, value-copy isolation, and no per-client idle goroutine; transport and consensus remain caller-owned. | Gap recovery, authorization, and bounded history. |

## Selection Policy

The next implementation should be chosen from a row whose public integration
boundary is clear and whose default behavior can remain unchanged. Storage,
replication, backup, and SQL planner changes require broader correctness tests;
generic `hat*` primitives are preferred when they provide a real reusable
contract rather than an unintegrated placeholder. Large or irreversible ideas
such as VShard migration, a new scripting runtime, or a consensus log remain
proposal-only until their recovery and security contracts are complete.
